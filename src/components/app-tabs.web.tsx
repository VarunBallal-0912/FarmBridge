// Legacy template file — not used by Farmbridge routing
// Farmbridge uses app/(tabs)/_layout.tsx with app/(tabs)/home.tsx etc.

import { View } from 'react-native';
export default function AppTabsWeb() {
  return <View />;
}
